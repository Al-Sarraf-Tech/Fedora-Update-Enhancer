# Generated by haskell-ci-orchestrator — DO NOT EDIT MANUALLY
name: Fedora-Update-Enhancer CI
on:
  push:
    branches:
      - main
    tags:
      - v*.*.*
      - v*
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * 1'
permissions:
  contents: write
  id-token: write
  security-events: write
concurrency:
  group: Fedora-Update-Enhancer-ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  repo-guard:
    name: Repository Context Guard
    runs-on: [self-hosted, dominus]
    timeout-minutes: 2
    steps:
      - name: Verify repository ownership
        run: |
          echo "Repository: ${{ github.repository }}"
          if [ "${{ github.repository }}" != "Al-Sarraf-Tech/Fedora-Update-Enhancer" ]; then
            echo "::error::SECURITY VIOLATION: Unauthorised repository."
            exit 1
          fi
          echo "Repository context verified."
  lint:
    name: Lint
    needs: repo-guard
    runs-on: [self-hosted, dominus]
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bash syntax check
        shell: bash
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs -I{} bash -n {}
      - name: ShellCheck
        shell: bash
        continue-on-error: true
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs shellcheck --severity=warning
      - name: Shell formatting (shfmt)
        shell: bash
        continue-on-error: true
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs shfmt -d
  test:
    name: Test
    runs-on: [self-hosted, dominus]
    needs: [repo-guard, lint]
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cache /tmp/docker-runner
        uses: actions/cache@v4
        with:
          key: ${{ runner.os }}-docker-runner-${{ github.sha }}
          path: /tmp/docker-runner
          restore-keys: ${{ runner.os }}-docker-runner-

      - name: Validate scripts
        shell: bash
        timeout-minutes: 1
        run: echo 'Lint-only repo — no test suite'
  security:
    name: Security
    runs-on: [self-hosted, dominus]
    needs: [repo-guard, lint]
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Secrets scan (gitleaks)
        shell: bash
        timeout-minutes: 10
        run: gitleaks detect --source=. --no-banner --exit-code=1
      - name: ShellCheck
        shell: bash
        continue-on-error: true
        timeout-minutes: 10
        run: find . -name '*.sh' -type f | xargs shellcheck --severity=warning
      - name: Shell formatting (shfmt)
        shell: bash
        continue-on-error: true
        timeout-minutes: 5
        run: find . -name '*.sh' -type f | xargs shfmt -d
  sbom:
    name: SBOM (CycloneDX)
    runs-on: [self-hosted, dominus]
    needs: [repo-guard, test]
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Generate CycloneDX SBOM
        shell: bash
        timeout-minutes: 5
        run: |
          mkdir -p dist
          scripts/sbom.sh
          jq -e '.bomFormat == "CycloneDX"' dist/sbom-*.json > /dev/null
          ls -la dist/
      - name: Upload SBOM artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ github.sha }}
          path: dist/sbom-*.json
          retention-days: 90
  integration:
    name: Integration
    runs-on: [self-hosted, dominus]
    needs: [repo-guard, test]
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: "Integration: syntax"
        shell: bash
        timeout-minutes: 30
        run: |
          echo 'Integration batch: syntax'
      - name: "Integration: mock-upgrade"
        shell: bash
        timeout-minutes: 30
        run: |
          echo 'Integration batch: mock-upgrade'
  perf-bench:
    name: Perf bench (15% p99 regression gate)
    runs-on: [self-hosted, dominus]
    needs: [repo-guard, lint]
    timeout-minutes: 15
    env:
      PERF_BENCH_RUNS: "20"
      PERF_BENCH_REGRESSION_PCT: "15"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bench current tree
        shell: bash
        timeout-minutes: 10
        run: |
          mkdir -p artifacts
          scripts/perf-bench.sh > artifacts/current-perf.json
          cat artifacts/current-perf.json
      - name: Compare against baseline (gate)
        shell: bash
        timeout-minutes: 5
        run: |
          if [[ ! -f tests/baseline-perf.json ]]; then
            echo "::warning::tests/baseline-perf.json missing - bench is recorded but not gated."
            exit 0
          fi
          scripts/perf-bench.sh compare tests/baseline-perf.json artifacts/current-perf.json
      - name: Upload bench artifact
        if: always()
        uses: actions/upload-artifact@v4
        env:
          SHA: ${{ github.sha }}
        with:
          name: perf-bench-${{ github.sha }}
          path: artifacts/current-perf.json
          retention-days: 30
  release:
    name: Release
    runs-on: [self-hosted, dominus]
    needs:
      - repo-guard
      - test
      - security
    if: startsWith(github.ref, 'refs/tags/')
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Generate checksums
        shell: bash
        timeout-minutes: 5
        run: cd release-assets && sha256sum * > SHA256SUMS
      - name: Create GitHub Release
        uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: release-assets/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
